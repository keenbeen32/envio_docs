# Envio Docs

Latest Envio documentation built with Mintlify.

## Prerequisites

- Node.js (>=20.0)
- pnpm

## Installation

- `pnpm install` - Install dependencies (includes Mint CLI for previewing locally)

## Commands

- `pnpm dev` - Preview locally
- `pnpm update-endpoints` - Update supported networks
